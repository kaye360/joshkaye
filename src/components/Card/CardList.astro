---
import type { HTMLAttributes } from 'astro/types';
interface Props extends HTMLAttributes<'div'> {}
---

<div class="grid gap-x-5 gap-y-8 grid-cols-[repeat(auto-fit,minmax(250px,1fr))] items-stretch">
    <slot />
</div>
