---
interface Props {
    /**
     * @prop class? : string,
     */
    props? : true
    class? : string,
}

const { class : className } = Astro.props
---

<div class:list={['p-2', className]} >
    <slot />
</div>
