---
import Wrapper from "../Base/Wrapper.astro";
import H1 from "./H1.astro";

interface Props {
    /**
     * @description H1 inside a Wrapper
     * @prop slot
     */
    props? : true
}
---
<Wrapper padding="inline" class="border-b border-primary-200 pb-3 mb-6">
    <H1>
        <slot />
    </H1>
</Wrapper>