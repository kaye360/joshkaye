---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'label'> {
    // 
}

const { class: className = '' } = Astro.props
---

<label 
    class:list={["block rounded-md py-2 px-2 w-full form-field-has-focus", className]}
    { ...Astro.props }
>
    <slot />
</label>